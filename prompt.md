用spec工作流，设计1米直径的管道的60cm宽的1mm厚度防腐层的圆周剥离设备的web版本的控制和管理软件。预估拉力3万牛，沿着管道一圈，左右宽度60cm剥离，且需要保证剥离时防腐层垂直于管道，并且将60cm的防腐层切为30条，每条2cm，在剥离过程中要全程记录30条分别的剥离的拉力和剥离的位置信息，不是恒力剥离，而是剥离时测量每一条的剥离力，一旦开始剥离，不需要再切割，因为防腐层在拉力足够时自己就能脱离。整个软件为网站，便于所有电脑使用。部署在vercel,技术架构为python。要设计完善功能，要非常工业化，高端，大气上档次，这是正式的国家管网的项目。要有大屏和各种完善的必要的所有功能。图表与可视化：实现折线图、饼图、分布图、柱状图等丰富图形图像，且图要非常高端大气现代化。若下面的技术方案与目标冲突，可调整技术方案。
布局与交互：大屏采用 网格化布局，支持模块化组合。
设计上要有「科技蓝 + 黑色背景 + 发光效果+动画效果」，高端大气现代化的工业化风格。
各模块需有流畅的过渡和动画效果。
后台与数据源
模拟或对接实时数据接口（例如 WebSocket / REST API）。
允许从数据库读取历史数据。。
二、非功能需求
安全性：系统需要保证数据的安全性，允许所有用户查看数据，但是修改需要管理员以及制定获得授权码的用户，授权码为test12，防止未经授权的修改操作，密码需加密存储在数据库。
可用性：系统需要具有良好的用户体验，操作极为简单易懂。
四、数据库需求
管理员表：包含管理员的基本信息，如用户名、密码。
用户信息表：包括用户名、手机号、密码、授权码（可选）等等；
其他必要的表；
2.2 技术解决方案
1. 系统架构设计
采用经典python的网站开发来实现系统。
2. 技术栈
后端：没有后端，后端全部基于neon的云数据库实现；
数据库：neon
部署：vercel
备注：
访问 Neon 数据库的方式：在没有传统后端的前提下，在 Vercel 上实现 Serverless Functions（使用环境变量安全地连接 Neon）;
Neon 数据库的初始结构：没有既定的表结构或字段命名规范；需要设计表及其字段；
UI 设计：无现成的风格指南或配色偏好；登录、注册、管理页面需要统一的移动优先响应式布局；
Neon 连接字符串在 Vercel 环境变量里。
管理员账号是需要预置一个默认账号a) 预置2个默认管理员账号（请提供初始账号信息），管理员1为admin，密码admin；管理员2为为admin1，密码admin1；管理员3为为admin2，密码admin2；
默认预置一个用户账号：用户名：user1；密码：user1
默认预置一个测试账号：用户名：test1；密码：test1
系统第一次运行时自动初始化数据库，若已经完成初始化数据库，则跳过本步骤。
若有不清楚的，请提问。
完成所有开发后部署到vercel，并且对部署的网站进行测试，直到测试通过。